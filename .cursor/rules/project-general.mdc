---
alwaysApply: true
---

# 项目规则

## 构建检查（按修改范围）

- **后端/.NET（如 `Cms.Abp` 下的 `.cs/.csproj` 等）**：**必须**执行 `dotnet build`
- **仅前端（如 `Cms.Vben5` 下的前端代码/资源）**：**不需要**执行 `dotnet build`
- **失败处理**：`dotnet build` 失败 → 修复后重跑，直到成功
- **文档**：需要新增/更新 md 文档 → 放在根目录 `/.docs`

## 文件操作（必须用命令行，保留历史）

- **复制/移动（迁移）**：用 `Copy-Item` / `Move-Item`（可用 `cp` / `mv`）
- **重命名**：用 `Rename-Item`（可用 `ren`）
- **禁止**：通过“读取原文件内容再写入新位置”的方式来迁移/重命名（会丢历史/低效）

```powershell
# 复制
Copy-Item "源路径" "目标路径" -Recurse
# 移动
Move-Item "源路径" "目标路径"
# 重命名
Rename-Item "原路径" -NewName "新名称"
```
